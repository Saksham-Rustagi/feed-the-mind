/**
 * Placeholder donation destination. Swap this for a real processor link
 * (PayPal, Donorbox, Stripe Payment Link, etc.) when one is available —
 * every donate CTA on the site reads from this single constant.
 */
export const DONATE_URL = "#";
